# Netlify config — build the Vite app and serve the SPA with a catch-all
# fallback so client-side routes (/shop, /product/:handle, /checkout) resolve.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
