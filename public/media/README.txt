Drop your hero assets here:

  hero.mp4         — the full-screen hero photoshoot video (autoplay/muted/loop)
  hero-poster.jpg  — poster frame shown before the video loads / on slow connections

The landing Hero (src/landing/sections/Hero.tsx) references:
  src   = /media/hero.mp4
  poster = /media/hero-poster.jpg

Until you add these, the hero shows a dramatic product shot as its background
fallback, so the page still looks finished.
